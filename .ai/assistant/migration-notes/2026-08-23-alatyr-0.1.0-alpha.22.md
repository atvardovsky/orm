# Alatyr Migration Note

Migration ID: `ALATYR-MIGRATION-20260823-alpha22`
Operation ID: `recheck-after-framework-update`
From framework version: `0.1.0-alpha.21`
To framework version: `0.1.0-alpha.22`
From adapter schema version: `20`
To adapter schema version: `21`
From template version: `21`
To template version: `22`
Prepared by: `Codex`
Prepared at: `2026-08-23`
Evidence basis: `mixed: source release review, generated migration diff, current adapter state`
Observed target branch: `alatyr-adapter-support-docs`
Observed target revision: `38df8a7ee5432ee23cdaf4ccb42a195c4f234c34 before uncommitted adapter update`
Migration assessment: `generated report from installed alpha.21 adapter state to AlatyrCore 958c87e`

## Routed Context

Affected canonical framework sources:

- `framework/subagent-delegation.md`

Affected task profiles or rule categories:

- `DELEGATION`
- `delegated execution`
- `fast focused coding`
- `large tasks`
- `parallel workstreams`

Candidate context intentionally omitted:

- Full Doctrine `src/`, `tests/`, and public docs: omitted because the update
  changed only Alatyr adapter/framework guidance, not ORM behavior.
- Full Alatyr framework corpus: omitted after the generated alpha.21-to-
  alpha.22 impact identified delegation as the only changed rule category.

## Changed Framework Rules

Added rules:

- none

Changed rules:

- `ALATYR-DELEGATION-001`

Removed or deprecated rules:

- none

## Required Target Actions

- Update `.ai/alatyr.yaml` from schema `20`, framework `0.1.0-alpha.21`,
  template `21` to schema `21`, framework `0.1.0-alpha.22`, template `22`.
- Replace installed `.ai/framework` files with the alpha.22 framework pack.
- Regenerate `.ai/assistant/bootstrap-index.json` from the updated manifest,
  project map, and context router.
- Update `.ai/assistant/extensions/lock.json` target baseline.
- Update versioned recovery/output templates and post-update guidance.
- Migrate delegation policy to schema `2`, including role catalog, enabled
  role IDs, retry policy, conflict policy, and normalized worker-result guard.
- Add project-owned worker orchestration prompt, execution-plan template,
  worker-result template, native-worker binding template, role catalog, and
  role prompts.
- Add the alpha.22 delegation capability fields to every assistant capability
  record and bind Codex support to observed `multi_agent_v1` tool evidence.
- Recheck adapter structural validation with migration-diff evidence.

## Optional Target Actions

- Reassess worker role enablement if the project decides to use more than
  `fast-focused-worker`.
- Run Doctrine runtime validation only if a later change touches `src/`,
  `tests/`, public docs, Composer metadata, or CI.
- Commit and publish only after a current explicit user request for those
  phases.

## Local Deviations

- Doctrine-specific source-of-truth, validation-command, PHP/composer, owner,
  and known-local-SQLite-gap facts are preserved.
- Delegation remains `suggest-only`; alpha.22 worker surfaces do not grant
  automatic delegation or broader action phases.
- Only Codex is marked currently supported for native delegation, based on
  observed tool evidence in this session. Other surfaces remain `unknown`.
- Backup owner remains unresolved and recorded as an adapter gap.

## Affected Target Surfaces

- `.ai/alatyr.yaml`: affected
- `.ai/framework`: affected
- `.ai/project`: not affected
- `.ai/assistant`: affected for bootstrap, extension baseline, migration,
  delegation policy, capability records, worker contracts, flow/overlay,
  worker templates, help/recovery/output templates, and post-update guidance
- `.ai/assistant/module-profile.md`: not modified; enabled module state
  remains current
- bridge files: not modified
- validation or manual review: affected; adapter validation rerun

## Approval And Validation

Approval needed: no separate protected-change approval; current user requested
an Alatyr adapter update and no Doctrine runtime, public behavior, security,
dependency, permission, live-service, destructive, commit, or publish action
was performed.

Approval record: not required for this adapter-only update.

Assessment completed before target changes: yes. The alpha.21-to-alpha.22
upgrade assessment was generated before target edits. Its initial strict
acceptance validation failed because the target had not yet been migrated,
then strict acceptance validation passed after the framework and delegation
surfaces were synchronized.

Validation run:

- `ALATYR_CORE_SOURCE=<AlatyrCore checkout> python3 tools/check_alatyr.py`
- `python3 <AlatyrCore checkout>/tools/validate_target_adapter.py --target <Doctrine ORM checkout> --framework-source <AlatyrCore checkout> --migration-diff <generated-migration-report> --validation-phase acceptance --output <generated-validation-report>`
- `jq empty` over updated JSON surfaces
- stale current-version scan for alpha.15-alpha.21 adapter/template
  references, excluding historical migration notes
- local absolute path scan
- `git diff --check`

## Final Evidence

Migration result: `complete for adapter structural state`
Remaining gaps: `backup owner is missing; Doctrine runtime validation skipped because no Doctrine runtime or public docs changed`
Residual risk: structural validation and migration diff evidence do not prove
semantic correctness of future assistant decisions, worker role quality, or
actual model identity for future delegated tasks.
