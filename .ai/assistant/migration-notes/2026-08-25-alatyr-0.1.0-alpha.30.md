# Alatyr Migration Note

Migration ID: `ALATYR-MIGRATION-20260825-alpha30`
Operation ID: `recheck-after-framework-update`
From framework version: `0.1.0-alpha.27`
To framework version: `0.1.0-alpha.30`
From adapter schema version: `25`
To adapter schema version: `28`
From template version: `25`
To template version: `28`
Prepared by: `Codex`
Prepared at: `2026-08-25`
Evidence basis: `mixed: generated migration diff and current adapter state`
Observed target branch: `alatyr-adapter-support-docs`
Observed target revision: `535fb46b04b69b64835aab70de521be6152ca218 before uncommitted adapter update`
Migration assessment: `temporary hash-bound alpha.27-to-alpha.30 assessment summarized in this note`

## Routed Context

Affected canonical framework sources:

- `framework/bridge-capability-matrix.md`
- `framework/context-profiles.md`
- `framework/debug-mode.md`
- `framework/guarantees.md`
- `framework/lifecycle.md`
- `framework/module-profile.md`
- `framework/operation-help.md`
- `framework/project-adapter-contract.md`
- `framework/project-knowledge.md`
- `framework/source-of-truth-registry.md`

Affected rule categories:

- `ADAPTER`
- `BRIDGE`
- `CONTEXT`
- `DEBUG`
- `EVIDENCE`
- `LIFECYCLE`
- `MODULE`
- `OPERATION`
- `PROJECT_KNOWLEDGE`
- `SOURCE`

Candidate context intentionally omitted:

- Doctrine `src/`, `tests/`, public docs, Composer metadata, and CI were
  omitted because this migration changes only Alatyr framework and adapter
  infrastructure, not ORM runtime behavior or public Doctrine contracts.
- Unchanged Alatyr canonical sources were omitted after the generated impact
  router identified the changed rules, framework files, schemas, and target
  template surfaces.

## Changed Framework Rules

Added rules:

- none

Changed rules:

- `ALATYR-ADAPTER-001`
- `ALATYR-BRIDGE-001`
- `ALATYR-CONTEXT-001`
- `ALATYR-DEBUG-001`
- `ALATYR-EVIDENCE-001`
- `ALATYR-KNOWLEDGE-001`
- `ALATYR-LIFECYCLE-001`
- `ALATYR-MODULE-001`
- `ALATYR-OPERATION-001`
- `ALATYR-SOURCE-001`

Removed or deprecated rules:

- none

## Required Target Actions

- Update the manifest to framework `0.1.0-alpha.30`, adapter schema `28`, and
  template `28`.
- Refresh the complete portable framework pack.
- Add truthful installation-state transition evidence for this legacy adapter
  and keep it `staged` until strict acceptance passes.
- Migrate all assistant capability records to schema 2 while preserving
  target evidence; add new registered surfaces as runtime-unverified.
- Keep Codex as the only selected assistant and record current instruction,
  skill, permission, diagram, and delegation evidence.
- Do not add Roo or Zed native bridges because those clients were not selected.
- Move Project Knowledge to contract 2 with guidance origin, decision-owner,
  exception, and explicit coverage semantics while preserving the empty index.
- Move new Debug authoring to contract and schema 4 while preserving historical
  records unchanged.
- Add delayed-outcome and adapter-maintenance evidence templates and update
  effectiveness and large-task guidance.
- Regenerate the compact bootstrap and recheck the installed adapter.

## Local Deviations

- Doctrine-specific authority, source-of-truth, validation-command, owner,
  commit-policy, and local SQLite facts were preserved.
- Existing assistant bridges were preserved as compatibility surfaces, but
  only Codex is selected and runtime-verified.
- Project-guidance and Debug indexes are empty on this branch, so no historical
  records required conversion.
- `tools/check_alatyr.py` remains the target-local wrapper around the external
  AlatyrCore checkout selected with `ALATYR_CORE_SOURCE`.

## Approval And Validation

Approval needed: no separate approval record. The current user explicitly
requested an Alatyr update, which authorizes adapter-only working-tree changes
and replacement of installed portable framework projections. No Doctrine
runtime, public behavior, security, dependency, live-service, commit, or
publication action was authorized or performed.

Assessment completed before target changes: yes. The generated assessment was
bound to branch `alatyr-adapter-support-docs` and revision
`535fb46b04b69b64835aab70de521be6152ca218`.

Validation run:

- Generated the alpha.27-to-alpha.30 framework, schema, and target-template
  migration report with `tools/report_migration_diff.py` from Alatyr Core.
- Ran `tools/plan_target_upgrade.py` in migration-staging mode before edits.
- Regenerated `.ai/assistant/bootstrap-index.json` from its canonical owners.
- Synchronized the full human operation reference with the canonical catalog
  and compact index: all 33 operation IDs, all 106 aliases, and all referenced
  operation flows are present and consistent.
- Ran `ALATYR_CORE_SOURCE=<AlatyrCore checkout> python3 tools/check_alatyr.py`.
- Ran focused Core Debug Mode and target-adapter-validator conformance checks;
  both pass, which confirms the source test set currently misses this
  schema-4 target-template assertion.
- Ran a temporary diagnostic copy of the alpha.30 validator with only its
  hard-coded Debug authoring-template expectation changed from 3 to 4; strict
  target validation then passed with zero errors and zero warnings.

Validation result: all target migration findings were resolved except
`DEBUG_MODE_TEMPLATE_VERSION`. Alatyr Core `0.1.0-alpha.30` ships
`.ai/assistant/templates/debug-session-record.json` with schema version 4 and
requires `debug_mode.contract_version: 4`, but its validator still hard-codes
that the template must use schema version 3. The target keeps the shipped
schema-4 contract and remains staged rather than weakening or falsifying it.

## Final Evidence

Migration result: `adapter content migrated; strict acceptance blocked by an Alatyr Core validator inconsistency`
Remaining gaps: `Core validator Debug template expectation; backup owner; runtime-unverified non-selected assistants`
Residual risk: `the adapter must be revalidated and its installation state moved from staged to accepted after the Core validator is synchronized`
