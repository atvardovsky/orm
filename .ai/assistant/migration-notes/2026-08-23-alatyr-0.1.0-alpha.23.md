# Alatyr Migration Note

Migration ID: `ALATYR-MIGRATION-20260823-alpha23`
Operation ID: `recheck-after-framework-update`
From framework version: `0.1.0-alpha.22`
To framework version: `0.1.0-alpha.23`
From adapter schema version: `21`
To adapter schema version: `22`
From template version: `22`
To template version: `23`
Prepared by: `Codex`
Prepared at: `2026-08-23`
Evidence basis: `mixed: generated migration diff and current adapter state`
Observed target branch: `alatyr-adapter-support-docs`
Observed target revision: `f9db39bed567b0c54abccabf13aa815d813e6339 before uncommitted adapter update`
Migration assessment: `temporary generated report summarized in this note`

## Routed Context

Affected canonical framework sources:

- `framework/debug-mode.md`
- `framework/engineering-evidence.md`

Affected task profiles or rule categories:

- `DEBUG`
- `ENGINEERING_EVIDENCE`
- `debug activation`
- `task observability`
- `final evidence`
- `human supervision measurement`

Candidate context intentionally omitted:

- Full Doctrine `src/`, `tests/`, public docs, Composer metadata, and CI:
  omitted because the update changes only Alatyr adapter/framework guidance,
  not ORM runtime behavior or public Doctrine behavior.
- Full Alatyr framework corpus: omitted after the generated alpha.22-to-
  alpha.23 impact identified Debug Mode and Engineering Evidence as the
  changed rule categories.

## Changed Framework Rules

Added rules:

- none

Changed rules:

- `ALATYR-DEBUG-001`
- `ALATYR-ENGINEERING-EVIDENCE-001`

Removed or deprecated rules:

- none

## Required Target Actions

- Update `.ai/alatyr.yaml` from schema `21`, framework `0.1.0-alpha.22`,
  template `22` to schema `22`, framework `0.1.0-alpha.23`, template `23`.
- Set `engineering_evidence.contract_version` and `debug_mode.contract_version`
  to `2`.
- Replace the changed installed `.ai/framework` files with the alpha.23
  framework pack files.
- Install version-2 Debug Mode and Engineering Evidence authoring templates.
- Move empty Debug Mode and Engineering Evidence indexes to schema `3`.
- Regenerate `.ai/assistant/bootstrap-index.json` from the updated manifest,
  project map, and context router.
- Recheck adapter structural validation with migration-diff evidence.

## Optional Target Actions

- Preserve historical schema-version-1 Debug and Engineering Evidence records
  unchanged if records are present on another branch; do not infer missing
  actor, causality, binding-state, or prior-binding facts.
- Run Doctrine runtime validation only if a later change touches `src/`,
  `tests/`, public docs, Composer metadata, or CI.
- Commit and publish only after a current explicit user request for those
  phases.

## Local Deviations

- Doctrine-specific source-of-truth, validation-command, PHP/composer, owner,
  and known-local-SQLite-gap facts are preserved.
- Debug and Engineering Evidence indexes are empty on this branch, so no
  historical record migration was required.
- Backup owner remains unresolved and recorded as an adapter gap.
- `tools/check_alatyr.py` remains the target-local wrapper around the external
  AlatyrCore checkout selected with `ALATYR_CORE_SOURCE`.

## Affected Target Surfaces

- `.ai/alatyr.yaml`: affected
- `.ai/framework`: affected for Debug Mode, Engineering Evidence, lifecycle,
  guarantees, installed operation help, file inventory, and rule registry
- `.ai/project`: affected only for empty Debug and Engineering Evidence index
  schema updates
- `.ai/assistant`: affected for bootstrap, migration note, and Debug/Evidence
  authoring templates
- `.ai/assistant/module-profile.md`: not modified; enabled module state remains
  current
- bridge files: not modified
- validation or manual review: affected; adapter validation rerun

## Approval And Validation

Approval needed: no separate protected-change approval; the current user
requested an Alatyr adapter update and no Doctrine runtime, public behavior,
security, dependency, permission, live-service, destructive, commit, or
publish action was performed.

Approval record: not required for this adapter-only update.

Assessment completed before target changes: yes. The alpha.22-to-alpha.23
upgrade assessment was generated before target edits. Its initial strict
acceptance validation failed because the target had not yet been migrated.

Validation run:

- `python3 <AlatyrCore checkout>/tools/plan_target_upgrade.py --target . --framework-source <AlatyrCore checkout> --output-dir <temporary-report-directory> --overwrite`
- `ALATYR_CORE_SOURCE=<AlatyrCore checkout> python3 tools/check_alatyr.py`
- `python3 <AlatyrCore checkout>/tools/validate_target_adapter.py --target . --framework-source <AlatyrCore checkout> --migration-diff <temporary-migration-report> --validation-phase acceptance`
- `jq empty` over updated JSON surfaces
- stale current-version scan for alpha.22/schema-21/template-22 adapter
  references, excluding historical migration notes and generated reports
- `git diff --check`

Post-update result: strict acceptance validation passed with `errors=0`,
`warnings=0`, and Alatyr adapter health `ready`.

## Final Evidence

Migration result: `complete for adapter structural state`
Remaining gaps: `backup owner is missing; generated migration reports are local evidence`
Residual risk: structural validation and migration diff evidence do not prove
semantic correctness of future assistant decisions or event attribution in
future Debug and Engineering Evidence records.
