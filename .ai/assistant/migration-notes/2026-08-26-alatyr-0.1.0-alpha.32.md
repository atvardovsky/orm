# Alatyr Migration Note

Migration ID: `ALATYR-MIGRATION-20260826-alpha32`
Operation ID: `recheck-after-framework-update`
From framework version: `0.1.0-alpha.31`
To framework version: `0.1.0-alpha.32`
From adapter schema version: `29`
To adapter schema version: `30`
From template version: `29`
To template version: `30`
To source revision: `138490a871802abe73958cea4da603cba4d26483`
Prepared by: `Codex`
Prepared at: `2026-08-26`
Evidence basis: `mixed: reviewed release migration report, branch-bound pre-change assessment, source Git history, generated context catalogs, and target validation`
Observed target branch: `alatyr-adapter-support-docs`
Observed target revision: `68a7b23a93b0849adb6f42b31410a04dc9290a56 before uncommitted alpha.32 adapter update`
Migration assessment: `temporary branch-bound assessment generated before alpha.32 changes and not retained in the target repository`

## Routed Context

Affected canonical framework sources:

- `framework/context-profiles.md`
- `framework/lifecycle.md`
- `framework/project-adapter-contract.md`
- `framework/project-vocabulary.md`

Affected rule categories:

- `ADAPTER`
- `CONTEXT`
- `LIFECYCLE`
- `VOCABULARY`

Selected target owners:

- `.ai/alatyr.yaml`
- `.ai/assistant/context-router.json`
- `.ai/assistant/module-profile.md`
- `.ai/project/source-of-truth-registry.md`
- `.ai/project/consistency-map.json`
- `.ai/project/blueprint.md`
- `.ai/assistant/gates/final-evidence.md`
- `.ai/assistant/flows/adapter-recheck.flow.md`

Project Knowledge routing returned no entries because the accepted target index
remains `enabled-empty`. No reusable guidance, promotion, route, or prior reuse
evidence was invented for this migration.

Candidate context intentionally omitted:

- Doctrine `src/`, `tests/`, public ORM docs, Composer metadata, dependencies,
  and CI were omitted because alpha.32 changes only Alatyr framework and
  adapter infrastructure.
- Historical Debug, Engineering Evidence, change-package, team-task, and
  project-guidance records were not loaded or rewritten because no current
  record was selected and their contracts did not change.

Context packet: `sha256:ae397659324b921c5b06e31224272d0c40302b8702569a42b8d10d7f80c3adfc`

- Index chain: `framework.root` through `framework.core`,
  `framework.lifecycle`, and `framework.knowledge`; `project.root` through
  `project.vocabulary`; `assistant.root` through `assistant.context`,
  `assistant.context-profiles`, and `assistant.gates`.
- Selected projection: 10 content items, 6 resolved semantic terms, and 11,056
  of 12,400 allowed words.
- The module profile, consistency map, blueprint, installation evidence, and
  adapter-recheck flow were inspected as recorded expansion but excluded from
  the deterministic packet to preserve the configured budget. Their affected
  sections were reconciled and included in final validation.

## Changed Framework Rules

Added rules:

- none

Changed rules:

- `ALATYR-ADAPTER-001`
- `ALATYR-CONTEXT-001`
- `ALATYR-LIFECYCLE-001`
- `ALATYR-VOCABULARY-001`

Removed or deprecated rules:

- none

## Required Target Actions

- Move the manifest to framework `0.1.0-alpha.32`, adapter schema `30`,
  template `30`, and context-router schema `8`.
- Synchronize the complete portable framework pack through Core revision
  `138490a871802abe73958cea4da603cba4d26483`.
- Install the framework context catalogs and semantic codebook without
  replacing Doctrine-owned vocabulary or project facts.
- Generate recursive context indexes from the exact installed framework,
  project, and assistant files, then regenerate the bootstrap from the
  manifest, project map, router, and semantic index.
- Install the deterministic context-packet contract and route material work
  through digest-bound item and semantic-term evidence.
- Reconcile the manifest, router, profiles, module state, root assistant entry
  points, gates, templates, blueprint, registry, consistency map, lifecycle
  state, extension baseline, and migration evidence.

## Target Adaptations

- Preserved the target profile budget of 12,400 words with 9,400 words
  reserved for Doctrine owner evidence.
- Raised the two-file bootstrap limit from 1,800 to 2,300 words because the
  installed `AGENTS.md` plus alpha.32 semantic preload measure 2,183 words.
  This records actual target cost and does not broaden the preloaded file set.
- Removed `.ai/alatyr.yaml` from the recursive framework-upgrade profile load
  list because it is a bootstrap owner outside the three contour roots; its
  version and state remain hash-bound into the generated bootstrap and are
  loaded directly for repair or migration.
- Removed the obsolete zero-byte `.ai/assistant/team/tasks/.gitkeep` after the
  generated context index became the tracked directory owner. The alpha.32
  renderer correctly rejects a zero-word content item, and no task record was
  removed.
- Kept the target project vocabulary separate from the portable semantic
  codebook. Semantic IDs abbreviate framework rules and cannot accept, replace,
  or reinterpret Doctrine terminology.

## Affected Target Surfaces

- `.ai/framework`: exact alpha.32 complete pack, including recursive catalogs
  and six semantic-codebook files.
- `.ai/alatyr.yaml`: schema, versions, source bindings, context contract,
  budget, lifecycle state, and local deviations.
- `.ai/assistant`: schema-8 router, recursive indexes, context packet,
  profiles, flows, gates, templates, module/extension state, installation
  evidence, migration note, and generated bootstrap.
- `.ai/project`: generated recursive indexes plus target-owned blueprint,
  source registry, and consistency relationship updates for the new routing
  infrastructure.
- `AGENTS.md` and `AI_ASSISTANTS.md`: bounded recursive discovery, semantic
  fallback, and final evidence instructions.
- bridge selection and runtime capability claims: unchanged; Codex remains the
  only selected assistant and non-selected surfaces remain unverified.

## Stateful Evidence Preservation

- Existing Debug, Engineering Evidence, Project Knowledge, change-package,
  extension, dependency, workspace-mode, and team records were preserved.
- The Project Knowledge index remains `enabled-empty`; no route or reuse claim
  was added.
- Prior installation-state transitions and migration notes remain immutable.
- The accepted alpha.31 state was reopened through the controlled-update
  lifecycle transition before alpha.32 acceptance.

## Approval And Authorization

Approval needed: no separate approval record. The repository owner explicitly
requested an Alatyr update, authorizing the adapter-only working-tree migration
including recursive routing and semantic infrastructure. The request does not
authorize commit, publication, Doctrine behavior changes, dependency changes,
or live external actions.

Protected behavior result: root assistant instructions and adapter routing
were updated only within the requested Alatyr scope. Doctrine behavior,
security policy, validation strength, privacy, retention, bridge selection,
external permissions, and publication boundaries were not weakened.

## Validation

- Generated the branch-bound pre-change migration assessment before target
  edits and reviewed the official alpha.31-to-alpha.32 migration report.
- Ran migration-staging validation after synchronization: zero errors, zero
  warnings, and zero blocking warnings; health correctly remained unverified
  while the lifecycle state was staged.
- Generated and validated 52 installed recursive context indexes. Content
  digests, word estimates, semantic references, traversal bounds, and the five
  bootstrap preload terms are structurally current.
- Verified the portable framework directory matches the selected Core checkout
  exactly and regenerated the hash-bound bootstrap.
- Ran the Core full source profile at revision `138490a871802abe73958cea4da603cba4d26483`:
  74 source checks and 147 unit tests passed.
- Final strict target acceptance completed with zero errors, zero warnings,
  and zero blocking warnings; adapter health is `ready`.
- Doctrine runtime tests were not run because no Doctrine runtime, test,
  dependency, public documentation, or CI file changed.

## Final Evidence

Migration result: `complete; strict acceptance passed and installation state is accepted`

Remaining gaps: `backup owner; enabled-empty Project Knowledge with no reuse evidence; runtime-unverified non-selected assistants; local SQLite SQRT limitation`

Residual risk: `recursive catalogs and semantic terms prove structural identity, not that every assistant client loaded, understood, or followed the selected context; provider token and cost effects remain unmeasured`

## Post-Update Doctrine Reality Reconciliation

After the framework migration, the repository owner requested a separate
adapter-only reality audit. The audit used Doctrine revision
`68a7b23a93b0849adb6f42b31410a04dc9290a56`, the current ignored
`composer.lock`, installed tool versions, canonical Doctrine docs/source/tests,
CI and validation configuration, `CODEOWNERS`, and direct local SQLite evidence.

Confirmed and corrected drift:

- Dependency knowledge now covers all 23 direct `require` and `require-dev`
  names. It adds `symfony/polyfill-php86` `v1.41.0`, updates Symfony Console
  and Cache to `v7.4.17`, binds exact distribution revisions, and records the
  SHA-256 digest of the current `composer.lock` bytes.
- Architecture and business-rule routing now include the documented offset
  and cursor pagination subsystem. The architecture catalog also represents
  cache and proxy areas already claimed by its human index.
- Public-behavior routing recognizes applicable Doctrine tutorials in addition
  to reference pages, and the consistency map covers all RST documentation.
- The Doctrine library workspace mode now includes the CI, database matrix,
  contribution/security, and root validation files named by its purpose.
- Architecture, documentation, testing, vocabulary, dependency, workspace,
  blueprint, business-logic, code-authoring, and bridge evidence was reviewed
  against the current base revision and dated `2026-08-26`.
- The SQLite `SQRT()` limitation remains real on SQLite `3.31.1`; both named
  tests reproduce it. The obsolete cached suite-size count was removed because
  it changes as Doctrine tests evolve.

Doctrine runtime source, tests, public documentation, dependency manifests,
the ignored lockfile, installed packages, security policy, and CI files were
not modified by this reconciliation. The adapter lifecycle was reopened at
sequence 5 and returned to `accepted` at sequence 6 after strict validation
reported zero errors, zero warnings, and zero blocking warnings. All 52
recursive indexes and the bootstrap were current; the portable framework was
an exact match; Composer validation and `git diff --check` passed; the
pagination suite passed 144 tests with 228 assertions and one platform skip;
and exact dependency identity comparison reported no drift.
