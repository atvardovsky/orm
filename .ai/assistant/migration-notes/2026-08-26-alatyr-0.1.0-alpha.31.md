# Alatyr Migration Note

Migration ID: `ALATYR-MIGRATION-20260826-alpha31`
Operation ID: `recheck-after-framework-update`
From framework version: `0.1.0-alpha.30`
To framework version: `0.1.0-alpha.31`
From adapter schema version: `28`
To adapter schema version: `29`
From template version: `28`
To template version: `29`
To source revision: `3883b9e28f2104be51abdfcf82ecfbe47a5ce3a5`
Prepared by: `Codex`
Prepared at: `2026-08-26`
Evidence basis: `mixed: reviewed release migration report, pre-change assessment, source Git history, and current target validation`
Observed target branch: `alatyr-adapter-support-docs`
Observed target revision: `5fe94af6b47262efbc2ac67eca8f73c83fac281f before uncommitted alpha.31 adapter update`
Migration assessment: `temporary branch-bound assessment generated before alpha.31 changes and not retained in the target repository`

## Routed Context

Affected canonical framework sources:

- `framework/debug-mode.md`
- `framework/engineering-evidence.md`
- `framework/project-knowledge.md`
- `framework/lifecycle.md`
- `framework/approval-records.md`
- `framework/bridge-capability-matrix.md`

Affected rule categories:

- `DEBUG`
- `ENGINEERING_EVIDENCE`
- `PROJECT_KNOWLEDGE`

Candidate context intentionally omitted:

- Doctrine `src/`, `tests/`, public ORM docs, Composer metadata, and CI were
  omitted because the migration changes only Alatyr framework and adapter
  infrastructure.
- Unchanged optional-module owners were omitted after the official migration
  report identified three changed rule families and their target consumers.

## Changed Framework Rules

Added rules:

- none

Changed rules:

- `ALATYR-DEBUG-001`
- `ALATYR-ENGINEERING-EVIDENCE-001`
- `ALATYR-KNOWLEDGE-001`

Removed or deprecated rules:

- none

## Required Target Actions

- Move the manifest to framework `0.1.0-alpha.31`, adapter schema `29`, and
  template `29`.
- Synchronize the complete portable framework pack through Core revision
  `3883b9e28f2104be51abdfcf82ecfbe47a5ce3a5`.
- Move new Debug authoring and its compact index to schema/contract version 5,
  including lifecycle phase coverage and project-knowledge candidate closure.
- Move Engineering Evidence authoring to schema/contract version 3 and its
  index to version 4, including reciprocal Debug session links.
- Move Project Knowledge to contract/index version 3 and record truthful
  `enabled-empty` adoption without fabricated reuse evidence.
- Synchronize affected flows, gates, summaries, READMEs, migration guidance,
  extension target baseline, module profile, installation evidence, and the
  generated bootstrap.

## Optional Target Actions

- none; no extension package, project-guidance promotion, Debug activation, or
  Doctrine behavior change was requested.

## Local Deviations

- Doctrine authority, source-of-truth records, validation commands, owner
  evidence, commit policy, and SQLite limitation remain unchanged.
- Codex remains the only selected assistant. Existing bridge capability
  evidence remains runtime-bound; canonical bridge-path ownership from the
  alpha.30 maintenance source is retained.
- `tools/check_alatyr.py` remains the target-local wrapper around the external
  Core checkout selected through `ALATYR_CORE_SOURCE`.

## Affected Target Surfaces

- `.ai/alatyr.yaml`: framework, schema, template, contract, gap, and accepted
  lifecycle state synchronized.
- `.ai/framework`: complete portable pack synchronized exactly.
- `.ai/project`: current Debug, Engineering Evidence, and Project Knowledge
  index contracts plus module documentation migrated.
- `.ai/assistant`: affected routes, flows, gates, templates, capability index,
  module profile, extension baseline, installation evidence, and bootstrap
  synchronized.
- `.ai/assistant/module-profile.md`: alpha.31 review and enabled-empty Project
  Knowledge adoption recorded.
- bridge files: unchanged; no assistant selection or observed capability
  changed.
- validation: strict alpha.31 acceptance performed against the current Core
  source and official release migration report.

## Stateful Evidence Migration

Debug record/index versions and preservation result:

- The empty target index moved from version 4 to version 5 and new authoring
  moved from schema 4 to schema 5. This branch contains no Debug records, so no
  historical record required lifecycle projection or mutation.

Engineering-evidence record/index versions and reciprocal Debug-link result:

- The empty target index moved from version 3 to version 4 and new authoring
  moved from schema 2 to schema 3. No evidence records exist on this branch, so
  no reciprocal link migration was required.

Project-knowledge index version, adoption state, and reuse-evidence result:

- The empty index moved from version 2 to version 3 with adoption state
  `enabled-empty` and an empty reuse-evidence list. No guidance reuse is
  claimed.

Historical records remain immutable: yes. Prior migration notes and the
alpha.30 installation-state transition were preserved unchanged. The
superseded, uncommitted same-version maintenance note was removed rather than
recorded as completed target history.

## Approval And Validation

Approval needed: no separate approval record. The current user explicitly
requested an Alatyr update, authorizing adapter-only working-tree changes but
not commit, publication, Doctrine behavior changes, dependency changes, or live
external actions.

Approval record: not required for this adapter-only migration because no
protected target behavior, retention/privacy rule, bridge selection, or gate
strength was changed.

Assessment completed before target changes: yes. The generated assessment was
bound to branch `alatyr-adapter-support-docs` and revision
`5fe94af6b47262efbc2ac67eca8f73c83fac281f`.

Validation run:

- Reviewed the official alpha.30-to-alpha.31 migration report and contract
  compatibility catalog.
- Generated a pre-change migration-staging assessment with
  `tools/plan_target_upgrade.py`.
- Compared Core revisions `5bd01b122` through `3883b9e28` and reconciled every
  changed framework, schema, and target-template surface.
- Verified the installed complete framework pack matches the current Core
  framework exactly.
- Regenerated and checked the compact bootstrap from its canonical owners.
- Ran strict target acceptance once while staged and once after the lifecycle
  transition. Both runs completed with zero errors, zero warnings, and zero
  blocking warnings; the final result reports adapter health `ready`.
- Ran the Core source's complete validation profile successfully at
  `3883b9e28`: 73 source checks and 139 unit tests passed.
- Parsed changed JSON/YAML, checked index/contract consistency and portable
  path hygiene, and ran Git whitespace validation.

## Final Evidence

Migration result: `complete; strict acceptance passed and installation state is accepted`

Remaining gaps: `backup owner; enabled-empty Project Knowledge with no reuse evidence; runtime-unverified non-selected assistants; local SQLite SQRT limitation`

Residual risk: `schema-5 Debug lifecycle and reciprocal Engineering Evidence links are structurally validated but have no target record instance on this branch yet`
