# Alatyr Migration Note

Migration ID: `ALATYR-MIGRATION-20260827-alpha33`
Operation ID: `recheck-after-framework-update`
From framework version: `0.1.0-alpha.32`
To framework version: `0.1.0-alpha.33`
From adapter schema version: `30`
To adapter schema version: `31`
From template version: `30`
To template version: `31`
To source revision: `1d87082eff62cff2a26e108992e4a0e5241f535b`
Prepared by: `Codex`
Prepared at: `2026-08-27`
Evidence basis: `reviewed release migration report, branch-bound pre-change assessment, source Git history, target-owned graph migration, generated derivatives, and strict target validation`
Observed target branch: `alatyr-adapter-support-docs`
Observed target revision: `68a7b23a93b0849adb6f42b31410a04dc9290a56 before uncommitted alpha.33 adapter update`
Migration assessment: `temporary branch-bound assessment generated before alpha.33 changes and not retained in the target repository`

## Routed Context

Profile: `framework-upgrade`
Areas: `assistant-adapter`, `project knowledge`, `consistency`, and `support information`
Operation: `recheck-after-framework-update`

Affected canonical framework sources:

- `framework/context-profiles.md`
- `framework/guarantees.md`
- `framework/lifecycle.md`
- `framework/logical-integrity.md`
- `framework/module-profile.md`
- `framework/operation-help.md`
- `framework/project-adapter-contract.md`
- `framework/source-of-truth-registry.md`
- `framework/support-information.md`

Project Knowledge routing was run initially and after concrete source evidence.
It selected no entries because the target remains `enabled-empty`; no reusable
guidance, promotion, route, or prior reuse evidence was invented.

Context packet: `sha256:3f5d7976164b42f80dc947ee897b3f3e97b3c5bcec7246767ece6e5024f9e142`

- Selected projection: 9 digest-verified content items, 5 resolved semantic
  terms, and 11,795 of 12,400 allowed words.
- The installation note, blueprint, business-logic owner, and selected
  consistency shards were inspected as recorded expansion outside the packet;
  their current digests are enforced by recursive catalogs and final adapter
  validation.

Doctrine runtime source, tests, public ORM documentation, dependencies, and CI
were omitted because alpha.33 changes only Alatyr support infrastructure and
does not alter Doctrine behavior. Historical Debug, Engineering Evidence,
change-package, team-task, and project-guidance records were preserved.

## Changed Framework Rules

Added rule: `ALATYR-SUPPORT-001`

Changed rules:

- `ALATYR-ADAPTER-001`
- `ALATYR-CONTEXT-001`
- `ALATYR-EVIDENCE-001`
- `ALATYR-INTEGRITY-001`
- `ALATYR-LIFECYCLE-001`
- `ALATYR-MODULE-001`
- `ALATYR-OPERATION-001`
- `ALATYR-SOURCE-001`

No rule was removed.

## Target Migration

- Synchronized the complete portable framework to alpha.33.
- Added the required target-owned support classification policy and generated
  support-state contract.
- Migrated the schema-2 inline consistency map to schema 3 while preserving
  all 13 fact IDs and 48 accepted path relationships.
- Represented prior path targets as explicit surface nodes across product,
  authoring, and assistant shards. No inferred relationship was accepted;
  the candidate register remains empty.
- Bound the support-policy-owned `.ai/**`, `AGENTS.md`, and `AI_ASSISTANTS.md`
  scope to the existing `alatyr-full-capability` node so every changed adapter
  support path can seed impact routing without creating a new relationship.
- Added the generated consistency reverse index for changed-path routing.
- Enabled guarded support-generation routing with an intentionally empty
  target registry because no target-owned generated artifact contract has
  been accepted. This installs the capability without inventing a generator.
- Updated operation, profile, help, gate, lifecycle, entry-point, and final
  evidence surfaces, then regenerated recursive context and bootstrap indexes.
- Generated support state last so it reflects the complete migrated adapter.

## Target Adaptations

- The support policy conservatively classifies every Git-visible `.ai/**`
  file and installed assistant entry point as an exact contract. This is
  intentionally coarse and does not claim semantic correctness.
- Existing schema-2 edges were migrated as accepted because they were already
  target-owned accepted state. Newly inferred node-to-node relationships were
  not added.
- The optional support-generation module is enabled with zero artifacts. Any
  future artifact requires an accepted generator owner, inputs, outputs, mode,
  validation, and approval trigger before it can be generated.
- Doctrine-specific facts and the prior alpha.32 reality reconciliation were
  preserved unchanged except where their routing references needed schema-3
  support.

## Approval And Authorization

The current user explicitly requested `update alatyr`. This authorizes
inspection and adapter-only working-tree modification for this migration. It
does not authorize commit, publication, Doctrine behavior changes, dependency
changes, or live external actions. No separate protected approval was needed:
the migration preserves target facts, validation strength, privacy, retention,
bridge selection, permissions, and publication boundaries.

## Validation

- The pre-change alpha.33 migration-staging assessment reported the expected
  3 errors and 22 blocking warnings for missing support and schema-31 surfaces.
- Migration-staging validation passed with zero errors, zero warnings, and zero
  blocking warnings after 55 recursive indexes, 52 consistency nodes, the
  operation/bootstrap projections, and a 366-file support snapshot were
  regenerated.
- Final strict acceptance was rerun after the accepted lifecycle transition
  and final support-state refresh: zero errors, zero warnings, zero blocking
  warnings, and adapter health `ready`.
- A final read-only support comparison reports zero differences; the generated
  state remains the canonical location for the current root digest.
- The bounded `HEAD` impact plan maps all 148 changed adapter paths, reaches 43
  nodes through 40 accepted edges, and reports zero skipped edges, unmapped
  paths, or relationship-candidate triggers.
- Alatyr Core source validation passed all 75 source checks and 161 unit tests
  at revision `1d87082eff62cff2a26e108992e4a0e5241f535b`.
- The target-local wrapper, generated consistency index, support-generation
  index, support snapshot, recursive catalogs, bootstrap, portable framework
  identity comparison, and `git diff --check` all pass.
- Doctrine runtime tests are not required because no Doctrine runtime, test,
  public documentation, dependency, or CI file is changed by this migration.

## Final Evidence

Migration result: `complete; final strict alpha.33 acceptance passed`

Remaining gaps: `backup owner; enabled-empty Project Knowledge with no reuse evidence; runtime-unverified non-selected assistants; local SQLite SQRT limitation; no accepted support-generation artifact bindings`

Residual risk: `support hashes and graph traversal provide bounded routing and structural freshness, not proof of semantic correctness, complete relationship coverage, or assistant-client compliance`
