# Alatyr Migration Note

Migration ID: `ALATYR-MIGRATION-20260824-alpha25`
Operation ID: `recheck-after-framework-update`
From framework version: `0.1.0-alpha.23`
To framework version: `0.1.0-alpha.25`
From adapter schema version: `22`
To adapter schema version: `24`
From template version: `23`
To template version: `24`
Prepared by: `Codex`
Prepared at: `2026-08-24`
Evidence basis: `mixed: generated migration diff and current adapter state`
Observed target branch: `alatyr-adapter-support-docs`
Observed target revision: `2d60ecaf17366bed467d3e92d8737088218b78f1 before uncommitted adapter update`
Migration assessment: `temporary generated report summarized in this note`

## Routed Context

Affected canonical framework sources:

- `framework/debug-mode.md`
- `framework/engineering-evidence.md`
- `framework/lifecycle.md`

Affected task profiles or rule categories:

- `DEBUG`
- `ENGINEERING_EVIDENCE`
- `LIFECYCLE`
- `architecture changes`
- `clean upstream projection`
- `cross-task effectiveness comparison`
- `debug activation`
- `final evidence`
- `framework upgrades`
- `human supervision measurement`
- `installation`
- `material semantic changes`
- `non-obvious defect repairs`
- `task observability`

Candidate context intentionally omitted:

- Full Doctrine `src/`, `tests/`, public docs, Composer metadata, and CI:
  omitted because the update changes only Alatyr adapter/framework guidance,
  not ORM runtime behavior or public Doctrine behavior.
- Full Alatyr framework corpus: omitted after the generated alpha.23-to-
  alpha.25 impact identified Debug Mode, Engineering Evidence, and Lifecycle
  as the changed rule categories.

## Changed Framework Rules

Added rules:

- none

Changed rules:

- `ALATYR-DEBUG-001`
- `ALATYR-ENGINEERING-EVIDENCE-001`
- `ALATYR-LIFECYCLE-001`

Removed or deprecated rules:

- none

## Required Target Actions

- Update `.ai/alatyr.yaml` from schema `22`, framework `0.1.0-alpha.23`,
  template `23` to schema `24`, framework `0.1.0-alpha.25`, template `24`.
- Set `debug_mode.contract_version` to `3`.
- Replace the changed installed `.ai/framework` files with the alpha.25
  framework pack files.
- Install schema-version-3 Debug Mode authoring, flow, gate, summary, and
  task-scale surfaces.
- Update Engineering Evidence capture flow, gate, and task-scale routing for
  the alpha.25 materiality and finalization contract.
- Move the empty Debug Mode compact index to schema `4`.
- Add `engineering-evidence` to the compact profile operation candidates for
  code, business, architecture, data, and security profiles so the operation
  catalog has a routable profile surface.
- Regenerate `.ai/assistant/bootstrap-index.json` from the updated manifest,
  project map, and context router.
- Recheck adapter structural validation with migration-diff evidence.

## Optional Target Actions

- Preserve historical schema-version-1 and version-2 Debug records unchanged
  if records are present on another branch; treat them as migration-limited
  evidence instead of inventing continuation, materiality, or claim-fidelity
  facts.
- Run Doctrine runtime validation only if a later change touches `src/`,
  `tests/`, public docs, Composer metadata, or CI.
- Commit and publish only after a current explicit user request for those
  phases.

## Local Deviations

- Doctrine-specific source-of-truth, validation-command, PHP/composer, owner,
  and known-local-SQLite-gap facts are preserved.
- Debug and Engineering Evidence indexes are empty on this branch, so no
  historical record migration was required.
- Backup owner remains unresolved and recorded as an adapter gap.
- `tools/check_alatyr.py` remains the target-local wrapper around the external
  AlatyrCore checkout selected with `ALATYR_CORE_SOURCE`.

## Affected Target Surfaces

- `.ai/alatyr.yaml`: affected
- `.ai/framework`: affected for Debug Mode, Engineering Evidence, Lifecycle,
  guarantees, context profiles, installed operations, file inventory, and rule
  registry
- `.ai/project`: affected only for the empty Debug index schema update
- `.ai/assistant`: affected for bootstrap, migration note, compact profile
  candidates, Debug/Evidence flows, gates, overlays, authoring templates, and
  update/installation notes
- `.ai/assistant/module-profile.md`: not modified; enabled module state remains
  current
- bridge files: not modified
- validation or manual review: affected; adapter validation rerun

## Approval And Validation

Approval needed: no separate protected-change approval; the current user
requested an Alatyr adapter update and no Doctrine runtime, public behavior,
security, dependency, permission, live-service, destructive, commit, or
publish action was performed.

Approval record: not required for this adapter-only update.

Assessment completed before target changes: yes. The alpha.23-to-alpha.25
upgrade assessment was generated before target edits. Its migration-staging
validation failed because the target had not yet been migrated.

Validation run:

- `python3 <AlatyrCore checkout>/tools/alatyr.py assess-upgrade --target <target repository> --framework-source <AlatyrCore checkout> --output-dir <temporary-report-directory> --validation-phase migration-staging --overwrite`
- `python3 <AlatyrCore checkout>/tools/render_target_bootstrap_index.py --target <target repository> --write`
- `ALATYR_CORE_SOURCE=<AlatyrCore checkout> python3 tools/check_alatyr.py`
- `python3 <AlatyrCore checkout>/tools/alatyr.py validate-adapter --target <target repository> --framework-source <AlatyrCore checkout> --validation-phase acceptance --migration-diff <temporary-migration-report> --json --output <temporary-acceptance-report>`
- `python3 <AlatyrCore checkout>/tools/render_target_bootstrap_index.py --target <target repository> --check`
- `git diff --check`

Post-update result: strict acceptance validation passed with `errors=0`,
`warnings=0`, and Alatyr adapter health `ready`.

## Final Evidence

Migration result: `complete for adapter structural state`
Remaining gaps: `backup owner is missing; generated assessment reports are local evidence`
Residual risk: structural validation and migration diff evidence do not prove
semantic correctness of future assistant decisions or event attribution in
future Debug and Engineering Evidence records.
