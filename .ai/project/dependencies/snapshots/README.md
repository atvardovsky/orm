# Dependency Snapshots

Committed dependency snapshots may be added here after license/privacy review. Current dependency knowledge is derived from `composer.json` and the local `composer.lock` fingerprint when present.
