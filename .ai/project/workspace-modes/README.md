# Doctrine ORM Workspace Modes

This directory contains enabled Alatyr workspace-mode routing for Doctrine ORM.

The accepted root mode is `doctrine-orm-library`, representing this checkout as
a PHP Composer library fork of Doctrine ORM. Additional modes may be added only
with reviewed `mode.json` records.
